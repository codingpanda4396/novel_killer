# 章节队列

## 文件用途

本文件用于维护下一批待生产章节。每一章必须先进入队列，再生成章节卡。

## 队列规则

- 每条队列项只描述章节任务，不写正文。
- 每章必须绑定至少一个主线推进点或人物关系变化。
- 章节状态只能从 `待规划` 到 `章节卡完成` 到 `草稿完成` 到 `审稿完成` 到 `定稿完成`。

## 当前队列

| 章号 | 工作标题 | 核心任务 | 必须承接 | 状态 |
| --- | --- | --- | --- | --- |
| 待定 | 待定 | 先完成前 8 万字修复规划 | `outlines/02_cold_start_0_80k.md` | 待规划 |

## 系统字段

- 文件职责：章节生产入口队列。
- 更新频率：每章生产前后更新。
- 使用阶段：章节卡生成、下一章规划。
- 禁止写入：章节正文。
